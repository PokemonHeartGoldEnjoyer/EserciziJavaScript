// scrivere un programma che dati
// il prezzo del biglietto
let biglietto = 16;

// e il numero di persone
let persone = 6;

// calcoli il totale da pagate
let calcolo;
calcolo = biglietto * persone;
frase = " Il totale da pagare è di " + calcolo + "€.";
// e lo scriva in una frase
console.log(frase);
